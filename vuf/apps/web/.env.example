# Vuf Web — переменные окружения (опционально)
# По умолчанию фронтенд ходит в API через Vite-прокси (см. vite.config.ts).
# При необходимости задать явный адрес API:
# VITE_API_BASE=http://localhost:4000/api/v1
