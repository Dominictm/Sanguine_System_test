# Задача для агента — [НАЗВАНИЕ ЗАДАЧИ]

> Заполняется перед делегированием чисто инженерной задачи (баг/рефакторинг/тест/
> фича без нарративной части) сабагенту через Agent-тул, отдельному worktree или
> внешнему/облачному агенту. Прикладывается вместе с [`AGENT_BRIEF.md`](AGENT_BRIEF.md).
> Для задач, требующих роли Рассказчика/сеттинга города — используй основной
> `CLAUDE.md`, этот бланк не подходит.

## Проект

**Sanguine System** — локальная система ведения хроник World of Darkness (V20).
Дев-контракт проекта — [`AGENT_BRIEF.md`](AGENT_BRIEF.md). Прочитай его перед началом работы.

---

## Активный город

**Город:** `<slug>` (папка `cities/<slug>/`)

---

## Задача

> _Опиши задачу здесь. Будь конкретен: что нужно создать / изменить / исправить._

**Тип задачи:** [ ] Новая фича  [ ] Рефакторинг  [ ] Баг  [ ] Тест

---

## Контекст

> _Что агент должен знать о задаче, чего нет в AGENT_BRIEF.md._
> Примеры:
> - Какая страница / компонент затронут (с file:line, если уже известно)
> - Какой эндпоинт API нужно добавить/изменить
> - Какие файлы точно нужно прочитать перед началом
> - Пример текущего поведения и желаемого

**Файлы для чтения перед стартом:**
- `AGENT_BRIEF.md`
- _(добавь конкретные файлы)_

---

## Ограничения

- НЕ трогать `web/.env`
- НЕ трогать `cities/*/archive/events.md` напрямую (только через `build_city_events.js`)
- НЕ менять `system/schema/card_schema.md` если задача не об этом
- НЕ менять `web/lib/parsers.js`, если задача явно не о парсинге
- НЕ добавлять npm-зависимости на клиентский JS без явного указания
- Канонные факты WoD — только по источникам; при неоднозначности — `// нужна сверка`
- _(добавь специфичные для этой задачи ограничения)_

---

## Критерии готовности

- [ ] _(Опиши конкретный результат — что должно работать)_
- [ ] Линтер карточек без ошибок: `node system/schema/validate_cards.js` _(если трогались карточки)_
- [ ] `npm test` в `web/` без новых падений _(если трогался веб)_
- [ ] Сервер стартует без ошибок _(если трогался бэкенд)_
- [ ] Тестовые данные, созданные для проверки, удалены
- [ ] _(Добавь свои критерии)_

---

## Что НЕ входит в задачу

> _Явно укажи, что агент не должен делать в рамках этой задачи, чтобы не было лишних изменений._

---

## Ожидаемый результат

> _Что должно быть на выходе: новые файлы, изменённые файлы, вывод команды, скриншот и т.д._
