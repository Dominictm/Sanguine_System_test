# Отчёт системного аналитика — Sanguine System

Дата: 2026-07-12
Метод: сверка с прошлым аудитом (`docs/audit/2026-07-09-project-improvement-plan.md`,
3 дня назад) + прямая проверка кода/тестов/зависимостей на актуальном `master`.
Не переоткрываю то, что уже закрыто и не изменилось с 07-09 — только дельта и
новое.

## Вердикт

Проект в здоровом состоянии: 341/341 тестов зелёные, 0 уязвимостей в
зависимостях, 0 TODO/FIXME/HACK в бэкенде. С прошлого отчёта закрыта большая
часть P0 (изоляция тестов, разбиение `modules.js`, первый кластер `scripts.js`)
и P1 (унификация ошибок, тесты на upload, `apiFetch`). Этот заход добавил
крупный, отдельно завершённый цикл: доступность модалок (17 модалок,
общий `openModal`/`closeModal` с фокус-трапом) + 6 находок дизайн-аудита
(контраст, переполнение `#info-panel`, реактивный `.stat-card`, touch-таргеты,
токены радиуса/отступов). Ничего горящего. Главный оставшийся структурный
риск — `scripts.js` (8379 строк) и `parsers.js` (1249 строк), оба уже
опознаны раньше и сознательно отложены поэтапно.

---

## Новое с 07-09 (найдено в этом заходе)

### P1 — утечка внутреннего текста ошибки в `tools.js:740`

```js
} catch (e) {
  res.status(500).json({ ok: false, errors: [e.message] });
}
```

Тот же класс бага, что чинили 07-09 в 11 местах (`serverError()` существует
и **уже используется в этом же файле** 5 раз — строки 609, 649, 680, 694, 800,
844), но эта конкретная точка (эндпоинт применения плана правок,
`POST /api/tools/...apply`, судя по контексту) была пропущена тогда — не
трогалась с 07-09 (`git log` подтверждает отсутствие изменений). `e.message`
здесь может содержать путь к файлу или фрагмент внутренней ошибки записи —
утекает клиенту напрямую вместо общего сообщения с логированием на сервере.

**Рекомендация:** заменить на `serverError(res, e)`, как и остальные 5 мест
в этом файле — однострочная правка, тот же паттерн.

### P2 — найден и исправлен во время кодревью этой сессии (отдельно от этого отчёта)

`web/public/utils.js:132` — двойной вызов `openModal(id)` без промежуточного
`closeModal(id)` терял ссылку на предыдущий `trapHandler`, утечка
`document`-level keydown-слушателя. Воспроизводимо через быстрый повторный
клик по карточке персонажа. Уже исправлено и покрыто отчётом кодревью в этой
же сессии — здесь только для полноты (не создавай задачу дважды).

---

## Подтверждено на месте (не новое, но перепроверено вживую)

- **Тесты**: `npm run test:unit` — 341/341, `web/tests/report.html` откачен
  после прогона (штатный артефакт, не коммитить).
- **`npm audit --omit=dev`**: 0 уязвимостей.
- **`tools/migrations/`**: по-прежнему только `README.md`, ни одной реальной
  миграции — соответствует рекомендации 07-09 «не трогать, пока не появится
  первая» — актуально, ничего не изменилось.
- **`release-overrides.json`**: по-прежнему 2 записи, риск не вырос.
- **god-файлы** (статус на момент этой записи, 07-12 до выполнения плана
  ниже): `web/public/scripts.js` — 8379 строк; `web/lib/parsers.js` —
  1249 строк, не тронут. **Оба закрыты этим же днём** — см. пункты
  3-5 «Рекомендуемого порядка» ниже: `scripts.js` → 2350 строк,
  `parsers.js` → `parsers/` (7 файлов + index).

## Ничего не изменилось (не требует внимания)

Всё из P0/P1/P2 списка 07-09, помеченное как «исправлено» или «отложено
осознанно» (полная песочница для тестов, `tools.js` CLI try/catch,
дублирование library-паттерна) — актуально в неизменном виде, перепроверять
не было смысла: ни один из этих файлов не менялся с 07-09 (кроме `tools.js`,
где правки шли в других участках файла, не в этих зонах).

---

## Рекомендуемый порядок

1. ~~**`tools.js:740` → безопасный фикс**~~ — сделано 2026-07-12 (коммит
   `5b307d5`). Не буквально `serverError(res, e)`, как было предложено выше:
   перед правкой проверил фронтенд-обработчик (`lsRunWrite` в scripts.js) —
   он читает `{ok, errors:[...]}`, а `serverError()` отдаёт `{error:'...'}`
   без `ok`; список ошибок в UI стал бы пустым. Сохранена форма ответа,
   какую реально читает вызывающий код, убрана только утечка `e.message`.
2. **Продолжить поэтапный вынос `scripts.js`** — прогресс 2026-07-12:
   вынесены 3 автономных кластера тем же приёмом, что `v20-sheet.js` 07-09
   (точный `sed`-вырез диапазона, проверка что ни один top-level вызов не
   ссылается за границу диапазона, живая проверка в headless Chrome):
   - `tour.js` (143 строки, коммит `d567801`) — онбординг-тур.
   - `log-session.js` (341 строка, коммит `5d9e705`) — вкладка «Сессия»
     Инструментов; заодно закоммичен ранее найденный, но не закоммиченный
     фикс утечки слушателя в `openModal()` (см. отчёт кодревью той же сессии).
   - `search.js` (139 строк, коммит `58292d6`) — глобальный поиск.
   - `city.js` (551 строка, коммит `b4d661f`) — сетка городов + модалка
     деталей/редактирования города (политика/фракции/локации). Потребовал
     правки одного source-guard теста (`all.test.js`), читавшего
     `CITY_SECTION_DEFS` из `scripts.js` по пути — перенаправлен на `city.js`.
   - `modules.js` (2673 строки, коммит `bb9b5c7`) — хроники/модули целиком:
     модалка деталей хроники, создание/заполнение модуля, рендер страницы
     модуля (сценарий, НПС, canon-check), редактирование записей сессии,
     генерация дневников. Не дробился дальше — весь кластер завязан на
     общие module-level переменные (`_chrDetailSlug`, `_modCreateStandalone`
     и т.п.), реальное разделение потребовало бы предметного анализа
     зависимостей, а не механического выреза; размер сопоставим с
     `v20-sheet.js` (2039 строк, вынесен 07-09) — тот тоже не дробился дальше.
   `scripts.js`: 8379 → 4532 строки за этот заход (более чем вдвое).
   Каждый вынос проверен вживую (CDP): auto-launch/навигация/реальный
   API-раунд-трип/клик-through в модалку/рендер формы — без новых ошибок
   консоли и сетевых сбоев.

   **Побочная находка при выносе `modules.js`** — не бага в моих правках,
   существовала до них: в `scripts.js` было ДВА top-level объявления
   `function mdToHtml(md)` — блочный парсер (свои классы `md-p`/`md-h*`/
   `md-table`) в начале файла и отдельный regex-based парсер (generic
   `<p>`/`<h2>`/`<ul>`, поддержка code-блоков и `resolveMdLink`) ближе к
   концу, внутри вынесенного диапазона. Повторное объявление функции в
   одной области видимости в JS полностью замещает предыдущее — значит
   ВСЕГДА, для всех вызовов из любого места, побеждало второе (позже
   выполняемое) объявление; первое было мёртвым кодом с самого начала.
   Поведение сохранено как есть (`modules.js` по-прежнему подключается
   после `scripts.js`, второе объявление продолжает побеждать) — решение,
   какой рендерер должен реально использоваться (и стоит ли удалять
   мёртвый первый), требует отдельного разбора, не механический вынос.
3. ~~**`parsers.js`**~~ — сделано 2026-07-12, план
   `docs/superpowers/plans/2026-07-12-scripts-decomposition-and-mdtohtml-fix.md`,
   Inline Execution. Разбит на `web/lib/parsers/` — 7 доменных файлов
   (`shared.js` коммит `f601281`, `city.js` коммит `41fe863`,
   `character.js` коммит `7abd879`, `location.js` коммит `aeb8620`,
   `scenario.js` коммит `2f117a0`, `threads.js` коммит `10e3e74`,
   `chronicle.js` коммит `5710f61`) + `index.js`, реэкспортирующий всё
   плоским объектом. `require('../lib/parsers')` резолвится через
   директорию без единой правки в `server.js` или 8 роутерах —
   проверено явным скриптом, сверяющим точный список импортов каждого
   потребителя (`server.js`/`chronicles.js`/`cities.js`/`archive.js`/
   `characters.js`/`locations.js`/`tools.js`/`threads.js`/
   `modules/shared.js`) против нового `index.js`. 341/341 тестов на
   каждом из 7 коммитов.
4. ~~**Дубликат `mdToHtml()`**~~ — исправлено 2026-07-12, тот же план,
   коммит `837646c`. Переименовал в `mdToHtmlBlock` (блочный парсер,
   классы `md-p`/`md-h*`) и `mdToHtmlPlain` (regex-парсер, generic-теги)
   и явно перенаправил все 11 точек вызова по тому, какой CSS реально
   ожидает обёртка (`.md-body` → Block, `.modp-md` → Plain). Проверено
   вживую до/после: `.md-body.chron-md` в хронике теперь рендерит
   `<p class="md-p">` (было — голый `<p>`), страницы модулей визуально
   не изменились. Побочно нашёл, что вызывающая функция одного из 4
   `.md-body`-сайтов (`scripts.js:2378`, внутри `openModuleDetail`) сама
   мёртвая — ничем не вызывается нигде в коде; не трогал (не в рамках
   этой правки), но стоит иметь в виду при следующей уборке.
5. ~~**Остальные кластеры `scripts.js`**~~ — сделано 2026-07-12, тот же
   план: `archive.js` (779 строк, коммит `a08fd3f`) — Threads/архив
   хроники/слухи/состояние мира; `char-detail.js` (1402 строки, коммит
   `e5a98c8`) — модалка персонажа целиком (карусель, редактирование
   полей, AI-генерация внешности/личности/биографии/диалогов/промтов).
   `scripts.js`: **8379 → 2350 строк** за всю сессию (снижение в 3.6
   раза). Оставшееся содержимое — City-switch/роутинг, Dashboard,
   Characters-грид, Tools/AI-настройки, Chronicles-страница, мёртвый
   `openModuleDetail`, Create Character Modal (~363 строки) — заметно
   более цельный «каркас приложения», чем то, что уже вынесено; следующий
   проход, если понадобится, — `Create Character Modal` (наиболее
   автономный из оставшихся), но специально его не искал — размен
   вынос/риск здесь уже не так очевиден, как был для `modules.js`/
   `char-detail.js`.
6. Остальное (P2 из 07-09) — оставить как есть, трогать только когда
   затронутый код и так меняется по другой причине.
